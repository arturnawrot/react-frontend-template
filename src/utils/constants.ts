/**
 * Shared design constants used across components
 */

/**
 * Standard container max-width for consistent layout across sections
 * Used by: SplitSection, FlippedM, CardSection, PropertySearch, AgentCarousel, FeaturedProperties
 */
export const CONTAINER_MAX_WIDTH = '1380px'

/**
 * Tailwind class for standard container max-width
 */
export const CONTAINER_MAX_WIDTH_CLASS = 'max-w-[1380px]'

/**
 * Standard horizontal padding for consistent spacing across sections
 * Used by: SplitSection, FlippedM, CardSection, PropertySearch, AgentCarousel, FeaturedProperties
 */
export const CONTAINER_PADDING_X = 'px-5'
